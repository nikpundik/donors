Initialize: 

	Run the application:
		npm install
		node Code/app.js

	Run the tests:
		(with jasmine installed or npm install jasmine)
		jasmine

Missing requirements:

	Donor address

Feedback:

	I found the assignment really interesting.
	Reading the job requests, I thought it would have been more centered on Meteor.
	Having no experience with Angular2 neither React, I tried to complete anyway the assignment to learn React.